import React, { PropTypes } from 'react';

const Home = () => {
    return (
        <div>
            <p>Welcome to the home screen</p>
        </div>
    );
};

Home.displayName = 'Home';

export default Home;
